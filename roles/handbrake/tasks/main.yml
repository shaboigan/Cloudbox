#########################################################################
#                   GNU General Public License v3.0                     #
#########################################################################
---
- name: "Set DNS Record on CloudFlare"
  include_role:
    name: cloudflare
  vars:
    subdomain: handbrake

- name: Slurp hashed htpasswd
  slurp:
    src: "/opt/traefik/.htpasswd"
  register: hashed

- name: Set hashed password
  set_fact:
    hashed_password: "{{ hashed['content'] | b64decode }}"

- name: Stop and remove any existing container
  docker_container:
    name: handbrake
    state: absent

- name: Create handbrake directories
  file: "path={{item}} state=directory mode=0775 owner={{user}} group={{user}}"
  with_items:
    - /opt/handbrake
    - /mnt/local/handbrake
    - /mnt/local/handbrake/source
    - /mnt/local/handbrake/source/yify
    - /mnt/local/handbrake/source/yify_anime
    - /mnt/local/handbrake/source/bluray
    - /mnt/local/handbrake/source/4k
    - /mnt/local/handbrake/source/dvd
    - /mnt/local/handbrake/output

- name: Create and start container
  docker_container:
    name: handbrake
    image: "jlesage/handbrake:latest"
    pull: yes
    published_ports:
      - "127.0.0.1:5800:5800"
    env:
      USER_ID: "{{uid}}"
      GROUP_ID: "{{gid}}"
      AUTOMATED_CONVERSION_PRESET: YIFY
#     AUTOMATED_CONVERSION_PRESET_2:
#     AUTOMATED_CONVERSION_PRESET_3:
#     AUTOMATED_CONVERSION_PRESET_4:
#     AUTOMATED_CONVERSION_PRESET_5:
      APP_NICENESS: "10"
    labels:
      traefik.enable: "true"
      traefik.backend: "handbrake"
      traefik.frontend.redirect.entryPoint: "https"
      traefik.frontend.rule: "Host:handbrake.{{domain}}"
      traefik.port: "5800"
      traefik.frontend.auth.basic: "{{ hashed_password }}"
      traefik.docker.network: "cloudbox"
      "com.github.cloudbox.cloudbox_managed": "true"
    volumes:
      - "/opt/handbrake:/config:rw"
      - "/mnt/local/handbrake/source/yify:/watch:rw"
      - "/mnt/local/handbrake/source/yify_anime:/watch2:rw"
      - "/mnt/local/handbrake/source/bluray:/watch3:rw"
      - "/mnt/local/handbrake/source/4k:/watch4:rw"
      - "/mnt/local/handbrake/source/dvd:/watch5:rw"
      - "/mnt/local/handbrake/output:/output:rw"
      - "/mnt/local/handbrake:/storage:ro"
    networks:
      - name: cloudbox
        aliases:
          - handbrake
    purge_networks: yes
    restart_policy: always
    state: started