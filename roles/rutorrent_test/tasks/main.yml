#########################################################################
# Title:         Cloudbox: ruTorrent Role                               #
# Author(s):     l3uddz, desimaniac, horjulf                            #
# URL:           https://github.com/cloudbox/cloudbox                   #
# Docker Image:  horjulf/rutorrent-autodl                               #
# --                                                                    #
#         Part of the Cloudbox project: https://cloudbox.works          #
#########################################################################
#                   GNU General Public License v3.0                     #
#########################################################################
---
- name: "Set DNS Record on CloudFlare"
  include_role:
    name: cloudflare
  vars:
    subdomain: rutorrent_test

- name: Slurp hashed htpasswd
  slurp:
    src: "/opt/traefik/.htpasswd"
  register: hashed

- name: Set hashed password
  set_fact:
    hashed_password: "{{ hashed['content'] | b64decode }}"

- name: Stop and remove any existing container
  docker_container:
    name: rutorrent_test
    state: absent

- name: Create rutorrent_test directory
  file: "path={{item}} state=directory mode=0775 owner={{user}} group={{user}} recurse=yes"
  with_items:
    - /opt/rutorrent_test

- name: Create new downloads directories
  file: "path={{item}} state=directory mode=0775 owner={{user}} group={{user}} recurse=yes"
  with_items:
    - "{{downloads.torrents}}/rutorrent_test"
    - "{{downloads.torrents}}/rutorrent_test/completed"
    - "{{downloads.torrents}}/rutorrent_test/incoming"
    - "{{downloads.torrents}}/rutorrent_test/watched"
  when: (torrents_downloads_path is defined)|default(false)

- name: Check if rtorrent.rc exists
  stat:
    path: "/opt/rutorrent_test/rtorrent/rtorrent.rc"
  register: rtorrent_rc

- name: Check if rtorrent.rc requires upgrading
  set_fact:
    standard_settings_needs_upgrading: "{{ (lookup('file', '/opt/rutorrent_test/rtorrent/rtorrent.rc')) is search('peer_exchange') }}"
    subdir_settings_exist: "{{ (lookup('file', '/opt/rutorrent_test/rtorrent/rtorrent.rc')) is search('/downloads/torrents/rutorrent_test/completed/') }}"
  when: rtorrent_rc.stat.exists

- name: Backup previous rtorrent.rc file
  shell: mv '/opt/rutorrent_test/rtorrent/rtorrent.rc' '/opt/rutorrent_test/rtorrent/rtorrent.rc.bak'
  ignore_errors: yes
  when: (rtorrent_rc.stat.exists) and (standard_settings_needs_upgrading|default(false,true))

- name: Check if plugins.ini exists
  stat:
    path: "/opt/rutorrent_test/rutorrent/settings/plugins.ini"
  register: plugins_ini

- name: Replace plugins.ini with customized one
  copy:
    src: plugins.ini
    dest: "/opt/rutorrent_test/rutorrent/settings/plugins.ini"
    owner: "{{user}}"
    group: "{{user}}"
    mode: 0775
    force: yes
  when: plugins_ini.stat.exists

- name: Set default_volumes variable
  set_fact:
    default_volumes:
      - "/etc/localtime:/etc/localtime:ro"
      - "/opt/rutorrent_test:/config"
      - "/opt/scripts:/scripts"
      - "/mnt:/mnt"

- name: Create and start container
  docker_container:
    name: rutorrent_test
    image: horjulf/rutorrent-autodl
    pull: yes
    published_ports:
      - "9080:9080"
      - "9443:9443"
      - "8118:8118"
      - "3000:3000"      
    env:
      PUID: "{{uid}}"
      PGID: "{{gid}}"
      VPN_ENABLED: "yes"
      VPN_PROV: "airvpn"
      ENABLE_PRIVOXY: "no"
      ENABLE_FLOOD: "no"
      ENABLE_AUTODL_IRSSI: "yes"
      LAN_NETWORK: "172.18.0.0/16"
      NAME_SERVERS:
      DEBUG: "false"
      PHP_TZ: "UTC"
      UMASK: "022"
    volumes: "{{ default_volumes + torrents_downloads_path|default([]) + old_rutorrent_downloads_path|default([]) }}"
    labels:
      traefik.enable: "true"
      traefik.backend: "rutorrent_test"
      traefik.frontend.redirect.entryPoint: "https"
      traefik.frontend.rule: "Host:rutorrent_test.{{domain}}"
      traefik.port: "80"
      traefik.frontend.auth.basic: "{{ hashed_password }}"
      traefik.docker.network: "cloudbox"
      "com.github.cloudbox.cloudbox_managed": "true"
    networks:
      - name: cloudbox
        aliases:
          - rutorrent_test
    purge_networks: yes
    restart_policy: unless-stopped
    stop_timeout: 300
    state: started

# Done for new setups (or setups requiring updating for updated rtorrent version).
- name: Standard Settings Tweaks
  include_tasks: "rutorrent_standard_settings.yml"
  when: (not continuous_integration)
        and
        ((not rtorrent_rc.stat.exists) or (standard_settings_needs_upgrading))

# Done for new setups (or setups requiring updating for updated rtorrent version).
- name: Subdir Settings Tweaks
  include_tasks: "rutorrent_subdir_settings.yml"
  when: (not continuous_integration)
        and
        ((not rtorrent_rc.stat.exists) or (standard_settings_needs_upgrading and subdirs_settings_exist))

- name: Start container to apply changes
  docker_container:
    name: rutorrent_test
    state: started
  when: (not continuous_integration)
        and
        ((not rtorrent_rc.stat.exists) or (standard_settings_needs_upgrading))
